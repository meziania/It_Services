{
  "name": "serviceit-scanner",
  "private": true,
  "version": "0.1.0",
  "description": "Personal freelance IT opportunity scanner (JobMaroc, Indeed, LinkedIn, Fiverr, Amazon) - scrape, score, contact, propose.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run start:dev --workspace=apps/api",
    "dev:web": "npm run dev --workspace=apps/web",
    "build": "npm run prisma:generate && npm run build --workspace=apps/api",
    "build:api": "npm run prisma:generate && npm run build --workspace=apps/api",
    "build:web": "npm run build --workspace=apps/web",
    "start:api": "npm run prisma:deploy && npm run start:prod --workspace=apps/api",
    "prisma:generate": "npm run generate --workspace=packages/database",
    "prisma:deploy": "npm run migrate:deploy --workspace=packages/database",
    "prisma:migrate": "npm run migrate:dev --workspace=packages/database",
    "prisma:studio": "npm run studio --workspace=packages/database",
    "docker:up": "docker compose -f docker/docker-compose.yml up -d",
    "docker:down": "docker compose -f docker/docker-compose.yml down"
  },
  "engines": {
    "node": ">=20"
  }
}
